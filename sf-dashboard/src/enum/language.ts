/* eslint-disable no-unused-vars */
// @ts-nocheck
// ts-prune-ignore-all

// Thêm để ko báo lỗi enum bị unused

export enum LANGUAGE {
  K = 'arb-lang',
}

export enum Locale {
  EN = 'en',
  VI = 'vi',
}
