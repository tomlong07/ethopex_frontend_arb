/* eslint-disable no-unused-vars */
// @ts-nocheck
// ts-prune-ignore-all

// Thêm để ko báo lỗi enum bị unused

export enum TType {
  ERROR = 'error',
  DEFAULT = 'default',
  PRIMARY = 'primary',
  INFO = 'info',
  SUCCESS = 'success',
  WARNING = 'warning',
}

export enum TSize {
  MEDIUM = 'medium',
  SMALL = 'small',
  LARGE = 'large',
}
