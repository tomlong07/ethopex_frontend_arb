/* eslint-disable no-unused-vars */
// @ts-nocheck
// ts-prune-ignore-all

// Thêm để ko báo lỗi enum bị unused

export enum GENERATE_MODE {
  PER_ANGLE = 'per_angle',
  PER_VARIATION = 'per_variation',
  GENERAL = 'general',
}

export enum INPUT_SOURCE {
  KEYWORDS = 'keyword_ai',
  TARGET_AUDIENCE = 'target_audience',
  CLONE_AD = 'clone_ads',
}

export enum TYPE_PROMPT {
  PROMPT = 'prompt',
  API = 'api',
}
