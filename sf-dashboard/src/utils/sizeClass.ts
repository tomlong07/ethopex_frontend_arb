const baseClasses =
  'hover:border-[#b2b8bf] focus:text-[#414141] focus:bg-white ' +
  'focus:border-[#86b7fe] focus:outline-none focus:ring-4 focus:ring-[#0d6efd40]'

export const sizeClasses = {
  lg: `h-[48px] text-base ${baseClasses}`,
  md: `h-[42px] text-sm ${baseClasses}`,
  sm: `h-[38px] text-xs ${baseClasses}`,
}
